import React from "react";

function Hello({name}) {
    return(
        <div>
            <h1>Hello, {name}</h1>
            <p>This is a simple functional component.</p>
        </div>
    );
}

export default Hello;